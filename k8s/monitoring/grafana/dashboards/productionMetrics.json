{
  "uid": "prod-environment-metrics",
  "title": "Production Environment Metrics",
  "tags": ["prod", "prometheus", "application"],
  "timezone": "browser",
  "schemaVersion": 36,
  "panels": [
    {
      "title": "CPU Usage per Container",
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 6, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "expr": "rate(container_cpu_usage_seconds_total{namespace='prod-namespace'}[1m])",
          "legendFormat": "{{pod}}"
        }
      ]
    },
    {
      "title": "Memory Usage per Container",
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 6, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "expr": "container_memory_usage_bytes{namespace='prod-namespace'}",
          "legendFormat": "{{pod}}"
        }
      ],
      "fieldConfig": { "defaults": { "unit": "bytes" } }
    },
    {
      "title": "Network I/O",
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 6, "w": 24, "x": 0, "y": 6 },
      "targets": [
        { "expr": "rate(container_network_receive_bytes_total{namespace='prod-namespace'}[5m])", "legendFormat": "RX" },
        { "expr": "rate(container_network_transmit_bytes_total{namespace='prod-namespace'}[5m])", "legendFormat": "TX" }
      ],
      "fieldConfig": { "defaults": { "unit": "bytes" } }
    }
  ]
}