services:
  app:
    build: .
    container_name: mynodeapp
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: dev
    restart: unless-stopped

  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: prometheusnodeapp
  #   ports:
  #     - "9091:9090"
  #   volumes:
  #     - ./prometheus/:/etc/prometheus/:ro
  #   networks:
  #     - monitoring

networks:
  monitoring:
    driver: bridge

# Note: The 'prometheus/' folder must exist in the project directory and contain prometheus.yml.
